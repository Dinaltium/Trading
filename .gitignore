# secrets
.env

# python
.venv/
__pycache__/
*.pyc

# tool/agent config, not part of the project build
.claude/
.agents/
skills-lock.json

# reference-only docs — not required to build/run the project
LINKS.md
claudeResult.md
chatGPTResult.md
notebookLLMResult.md
notebookLLMResultPipeline.md

# generated at runtime, not build-relevant — EXCEPT audit_log.jsonl, which is pushed
# deliberately so the deployed dashboard can read it (see dashboard/ + src/audit_log.py)
logs/*
!logs/audit_log.jsonl

# Next.js dashboard
dashboard/node_modules/
dashboard/.next/
dashboard/.vercel/
dashboard/next-env.d.ts
